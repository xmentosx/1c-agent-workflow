# ai_rules_1c r20 qualification

The controlled fork release `itl-main-5f3d3f0-r20` is built directly from upstream `5f3d3f03b778d7de38cf2cfb18a20cf3e7ed79d8` and published at exact fork commit `151aa980b5e99b3d129e974925e734d9ef0afa3e`.

The intake accepts the new upstream model profiles, standard verification depth, SUPPORT_GUARD/support-edit, XDTO/UUID/BSP tools, expanded metadata DSL, DT operations, UI-testing routes, conditional Gate 3a and subagent contracts. ITL-specific resolutions retain the single-client installer, delegated MCP ownership, `.dev.env` preservation and branch-local lifecycle guards.

Fork qualification passed upstream `validate-rules -Strict`, targeted fork tests, builder `Verify`, and Full `69/69` on the final clean commit. Workflow compatibility is qualified separately by the workflow Full gate with this exact clean checkout; workflow Release, live PM5 smoke and publication to `master` are outside this update.

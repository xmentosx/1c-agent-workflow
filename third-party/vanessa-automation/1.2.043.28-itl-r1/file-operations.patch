diff --git a/VanessaAutomation/Forms/MCPVA/Ext/Form/Module.bsl b/VanessaAutomation/Forms/MCPVA/Ext/Form/Module.bsl
index ece19a947..8d18e639d 100644
--- a/VanessaAutomation/Forms/MCPVA/Ext/Form/Module.bsl
+++ b/VanessaAutomation/Forms/MCPVA/Ext/Form/Module.bsl
@@ -5428,29 +5428,247 @@
 КонецПроцедуры
 	
 &НаКлиенте
-Функция MCPНормализоватьПутьКФичаФайлу(Путь)
-	
-	Если Не ЗначениеЗаполнено(Путь) Тогда
-		ВызватьИсключение Ванесса.Локализовать("Не указан путь к .feature файлу.");
+Функция MCPЭтоАбсолютныйПуть(Путь)
+
+	Если Лев(Путь, 1) = "/" ИЛИ Лев(Путь, 1) = "\" Тогда
+		Возврат Истина;
 	КонецЕсли;
-	
-	НормализованныйПуть = Ванесса.ПреобразоватьПутьСТочкамиКНормальномуПути(Строка(Путь));
-	Если Не ЗначениеЗаполнено(НормализованныйПуть) Тогда
-		ВызватьИсключение Ванесса.Локализовать("Не удалось определить путь к .feature файлу.");
+
+	Возврат СтрДлина(Путь) > 2 И Сред(Путь, 2, 1) = ":";
+
+КонецФункции
+
+&НаКлиенте
+Функция MCPПутьИмеетНедопустимыйСинтаксис(Путь)
+
+	Если Не ЗначениеЗаполнено(Путь)
+		Или Найти(Путь, Символы.ПС) > 0
+		Или Найти(Путь, Символы.ВК) > 0
+		Или Найти(Путь, Символы.Таб) > 0 Тогда
+		Возврат Истина;
+	КонецЕсли;
+
+	Если Ванесса.ЭтоLinux Тогда
+		Возврат Найти(Путь, Символ(0)) > 0;
 	КонецЕсли;
+
+	ПроверяемаяЧасть = Путь;
+	Если СтрДлина(Путь) > 2 И Сред(Путь, 2, 1) = ":" Тогда
+		ПроверяемаяЧасть = Сред(Путь, 3);
+	КонецЕсли;
+
+	Возврат Найти(ПроверяемаяЧасть, ":") > 0
+		Или Найти(ПроверяемаяЧасть, """") > 0
+		Или Найти(ПроверяемаяЧасть, "*") > 0
+		Или Найти(ПроверяемаяЧасть, "?") > 0
+		Или Найти(ПроверяемаяЧасть, "<") > 0
+		Или Найти(ПроверяемаяЧасть, ">") > 0
+		Или Найти(ПроверяемаяЧасть, "|") > 0;
+
+КонецФункции
+
+&НаКлиенте
+Функция MCPПодготовитьПутьКФичам(Путь)
 	
-	Если Не Ванесса.ФайлСуществуетКомандаСистемы(НормализованныйПуть) Тогда
-		ВызватьИсключение Ванесса.Локализовать("Файл не найден:") + " " + НормализованныйПуть;
+	ИсходныйПуть = СокрЛП(Строка(Путь));
+	Если MCPПутьИмеетНедопустимыйСинтаксис(ИсходныйПуть) Тогда
+		Возврат Новый Структура("Статус,ПолныйПуть", "PATH_INVALID", "");
 	КонецЕсли;
 	
-	Если Ванесса.ЭтоКаталогКомандаСистемы(НормализованныйПуть) Тогда
-		ВызватьИсключение Ванесса.Локализовать("Ожидался путь к .feature файлу, а не к каталогу:") + " " + НормализованныйПуть;
+	Если Найти(ИсходныйПуть, "$workspaceRoot") > 0 Тогда
+		ИсходныйПуть = СтрЗаменить(ИсходныйПуть, "$workspaceRoot", Ванесса.ДополнитьСлешВПуть(Ванесса.Объект.КаталогПроекта));
 	КонецЕсли;
 	
-	Возврат НормализованныйПуть;
+	Если Найти(ИсходныйПуть, "$instrumentsRoot") > 0 И ЗначениеЗаполнено(Ванесса.Объект.КаталогИнструментов) Тогда
+		ИсходныйПуть = СтрЗаменить(ИсходныйПуть, "$instrumentsRoot", Ванесса.ДополнитьСлешВПуть(Ванесса.Объект.КаталогИнструментов));
+	КонецЕсли;
+
+	Если Не Ванесса.ЭтоLinux Тогда
+		ИсходныйПуть = СтрЗаменить(ИсходныйПуть, "/", "\");
+	КонецЕсли;
+
+	Если Не MCPЭтоАбсолютныйПуть(ИсходныйПуть)
+		И ЗначениеЗаполнено(Ванесса.Объект.КаталогПроекта) Тогда
+		ИсходныйПуть = Ванесса.ДополнитьСлешВПуть(Ванесса.Объект.КаталогПроекта) + ИсходныйПуть;
+	КонецЕсли;
+
+	Возврат Новый Структура("Статус,ПолныйПуть", "OK", ИсходныйПуть);
 	
 КонецФункции
 
+&НаКлиенте
+Функция MCPНормализоватьПутьКФичаФайлу(Путь)
+
+	ПроверкаПути = MCPПодготовитьПутьКФичам(Путь);
+	Если ПроверкаПути.Статус <> "OK" Тогда
+		ВызватьИсключение ПроверкаПути.Статус + ": " + Строка(Путь);
+	КонецЕсли;
+
+	Возврат ПроверкаПути.ПолныйПуть;
+
+КонецФункции
+
+&НаКлиенте
+Процедура MCPЗавершитьПроверкуПутиСОшибкой(ДанныеПроверки, Статус)
+
+	Если Статус = "PATH_NOT_FOUND" Тогда
+		ТекстОшибки = Ванесса.Локализовать("Файл или каталог не найден:")
+			+ " " + ДанныеПроверки.ПолныйПуть;
+	ИначеЕсли Статус = "PATH_ACCESS_DENIED" Тогда
+		ТекстОшибки = Ванесса.Локализовать("Нет доступа к файлу или каталогу:")
+			+ " " + ДанныеПроверки.ПолныйПуть;
+	Иначе
+		ТекстОшибки = Ванесса.Локализовать("Некорректный путь к файлу или каталогу:")
+			+ " " + ДанныеПроверки.ПолныйПуть;
+	КонецЕсли;
+
+	МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(
+		ДанныеПроверки.Запрос.Идентификатор, Статус, ТекстОшибки);
+
+КонецПроцедуры
+
+&НаКлиенте
+Процедура MCPПродолжитьОткрытиеФичаФайла(ДанныеПроверки)
+
+	МодульМсп_Сервер = МодульМсп_Сервер();
+	Идентификатор = ДанныеПроверки.Запрос.Идентификатор;
+	Ванесса.УстановитьИдентификаторЗадачиMCP(Идентификатор);
+	МодульМсп_Сервер.УведомитьОПрогрессе(
+		Идентификатор, 0, 3, Ванесса.Локализовать("Загрузка feature файла."));
+
+	MCPOpenFeatureFileВРедакторе(
+		ДанныеПроверки.ПолныйПуть,
+		ДанныеПроверки.ТипВызова,
+		ДанныеПроверки.ДопПараметрыВызоваMCP);
+
+КонецПроцедуры
+
+&НаКлиенте
+Процедура MCPПроверкаЧтенияФайлаЗавершена(ДанныеПроверки) Экспорт
+
+	MCPПродолжитьОткрытиеФичаФайла(ДанныеПроверки);
+
+КонецПроцедуры
+
+&НаКлиенте
+Процедура MCPПроверкаТипаПутиЗавершена(ЭтоКаталог, ДанныеПроверки) Экспорт
+
+	Если ЭтоКаталог Тогда
+		Если ДанныеПроверки.РазрешитьКаталог Тогда
+			ДанныеПроверки.ЭтоКаталог = Истина;
+			MCPПродолжитьОткрытиеФичаФайла(ДанныеПроверки);
+			Возврат;
+		КонецЕсли;
+		MCPЗавершитьПроверкуПутиСОшибкой(ДанныеПроверки, "PATH_INVALID");
+		Возврат;
+	КонецЕсли;
+
+	ДанныеПроверки.Стадия = "READ";
+	ТекстовыйДокумент = Новый ТекстовыйДокумент;
+	ДанныеПроверки.ТекстовыйДокумент = ТекстовыйДокумент;
+	ОписаниеОповещения = Новый ОписаниеОповещения(
+		"MCPПроверкаЧтенияФайлаЗавершена", ЭтаФорма, ДанныеПроверки,
+		"MCPОшибкаПроверкиПути", ЭтаФорма);
+	ТекстовыйДокумент.НачатьЧтение(
+		ОписаниеОповещения, ДанныеПроверки.ПолныйПуть, КодировкаТекста.UTF8);
+
+КонецПроцедуры
+
+&НаКлиенте
+Процедура MCPПроверкаПутиЗавершена(НайденныеФайлы, ДанныеПроверки) Экспорт
+
+	Если ДанныеПроверки.ЭтоКаталог Тогда
+		MCPПродолжитьОткрытиеФичаФайла(ДанныеПроверки);
+		Возврат;
+	КонецЕсли;
+
+	Если НайденныеФайлы.Количество() = 0 Тогда
+		MCPЗавершитьПроверкуПутиСОшибкой(ДанныеПроверки, "PATH_NOT_FOUND");
+		Возврат;
+	КонецЕсли;
+
+	ДанныеПроверки.ПолныйПуть = НайденныеФайлы[0].ПолноеИмя;
+	ДанныеПроверки.Стадия = "TYPE";
+	ДанныеПроверки.НайденныйФайл = НайденныеФайлы[0];
+	ОписаниеОповещения = Новый ОписаниеОповещения(
+		"MCPПроверкаТипаПутиЗавершена", ЭтаФорма, ДанныеПроверки,
+		"MCPОшибкаПроверкиПути", ЭтаФорма);
+	ДанныеПроверки.НайденныйФайл.НачатьПроверкуЭтоКаталог(ОписаниеОповещения);
+
+КонецПроцедуры
+
+&НаКлиенте
+Процедура MCPОшибкаПроверкиПути(
+		ИнформацияОбОшибке, СтандартнаяОбработка, ДанныеПроверки) Экспорт
+
+	СтандартнаяОбработка = Ложь;
+	Если ДанныеПроверки.Стадия <> "SEARCH" Тогда
+		Статус = "PATH_ACCESS_DENIED";
+	Иначе
+		ТекстОшибки = НРег(КраткоеПредставлениеОшибки(ИнформацияОбОшибке));
+		Если Найти(ТекстОшибки, "доступ") > 0
+			Или Найти(ТекстОшибки, "access") > 0
+			Или Найти(ТекстОшибки, "denied") > 0 Тогда
+			Статус = "PATH_ACCESS_DENIED";
+		Иначе
+			Статус = "PATH_NOT_FOUND";
+		КонецЕсли;
+	КонецЕсли;
+
+	MCPЗавершитьПроверкуПутиСОшибкой(ДанныеПроверки, Статус);
+
+КонецПроцедуры
+
+&НаКлиенте
+Процедура MCPНачатьПроверкуПути(
+		ПутьКФайлу, Запрос, ТипВызова, ДопПараметрыВызоваMCP = Неопределено)
+
+	Ванесса.УстановитьИдентификаторЗадачиMCP(Запрос.Идентификатор);
+	ПодготовкаПути = MCPПодготовитьПутьКФичам(ПутьКФайлу);
+	Если ПодготовкаПути.Статус <> "OK" Тогда
+		ДанныеОшибки = Новый Структура("Запрос,ПолныйПуть", Запрос, Строка(ПутьКФайлу));
+		MCPЗавершитьПроверкуПутиСОшибкой(ДанныеОшибки, ПодготовкаПути.Статус);
+		Возврат;
+	КонецЕсли;
+
+	РазрешитьКаталог = ТипВызова = "ОткрытьЗагрузитьИлиПерезагрузитьФичаФайл";
+	ЭтоКаталог = РазрешитьКаталог И НРег(Прав(ПодготовкаПути.ПолныйПуть, 8)) <> ".feature";
+	ДанныеПроверки = Новый Структура(
+		"Запрос,ТипВызова,ДопПараметрыВызоваMCP,ПолныйПуть,РазрешитьКаталог,ЭтоКаталог,Стадия,ТекстовыйДокумент,НайденныйФайл",
+		Запрос, ТипВызова, ДопПараметрыВызоваMCP, ПодготовкаПути.ПолныйПуть,
+		РазрешитьКаталог, ЭтоКаталог, "SEARCH", Неопределено, Неопределено);
+
+	ОписаниеОповещения = Новый ОписаниеОповещения(
+		"MCPПроверкаПутиЗавершена", ЭтаФорма, ДанныеПроверки,
+		"MCPОшибкаПроверкиПути", ЭтаФорма);
+
+	Если ЭтоКаталог Тогда
+		НачатьПоискФайлов(
+			ОписаниеОповещения, ДанныеПроверки.ПолныйПуть, "*.feature", Ложь);
+		Возврат;
+	КонецЕсли;
+
+	ПозицияРазделителя = СтрДлина(ДанныеПроверки.ПолныйПуть);
+	Пока ПозицияРазделителя > 0 Цикл
+		ТекущийСимвол = Сред(ДанныеПроверки.ПолныйПуть, ПозицияРазделителя, 1);
+		Если ТекущийСимвол = "/" Или ТекущийСимвол = "\" Тогда
+			Прервать;
+		КонецЕсли;
+		ПозицияРазделителя = ПозицияРазделителя - 1;
+	КонецЦикла;
+
+	Если ПозицияРазделителя = 0 Тогда
+		КаталогПоиска = ".";
+		МаскаПоиска = ДанныеПроверки.ПолныйПуть;
+	Иначе
+		КаталогПоиска = Лев(ДанныеПроверки.ПолныйПуть, ПозицияРазделителя);
+		МаскаПоиска = Сред(ДанныеПроверки.ПолныйПуть, ПозицияРазделителя + 1);
+	КонецЕсли;
+
+	НачатьПоискФайлов(ОписаниеОповещения, КаталогПоиска, МаскаПоиска, Ложь);
+
+КонецПроцедуры
+
 &НаКлиенте
 Процедура MCPOpenFeatureFileВРедакторе(ИмяФайла, ТипВызова, ДопПараметрыВызоваMCP = Неопределено)
 	
@@ -5909,32 +6126,8 @@
 Процедура ОткрытиеФичаФайлаВРедакторе(ПутьКФайлу, Запрос, ТипВызова, ДопПараметрыВызоваMCP = Неопределено)
 	
 	Если ЗначениеЗаполнено(ПутьКФайлу) Тогда
-		Если НЕ Ванесса.ФайлСуществуетКомандаСистемы(ПутьКФайлу) Тогда
-			
-			ПолныйПутьКФайлуВКаталогеПроекта = Ванесса.ДополнитьСлешВПуть(Ванесса.Объект.КаталогПроекта) + ПутьКФайлу;
-			Если НЕ Ванесса.ФайлСуществуетКомандаСистемы(ПолныйПутьКФайлуВКаталогеПроекта) Тогда
-				МодульМсп_Сервер = МодульМсп_Сервер();
-				ТекстОшибки = Ванесса.Локализовать("Файл <%1> не был найден.");
-				Если НЕ Ванесса.ЭтоLinux И Найти(ПутьКФайлу, ":") = 0 Тогда
-					ТекстОшибки = Ванесса.Локализовать("Файл <%1> не был найден. В каталоге проекта файл <%2> тоже не был найден. Видимо был передан относительный путь, попробуйте передать полный путь к файлу.");
-				КонецЕсли;
-				
-				ТекстОшибки = Ванесса.ПодставитьПараметрыВСтроку(ТекстОшибки, ПутьКФайлу, ПолныйПутьКФайлуВКаталогеПроекта);
-				МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Запрос.Идентификатор, "INVALID_PARAMS", ТекстОшибки);
-				Возврат;
-			Иначе
-				ПутьКФайлу = ПолныйПутьКФайлуВКаталогеПроекта;
-			КонецЕсли;	
-			
-		КонецЕсли;	
-		ПутьКФайлу = MCPНормализоватьПутьКФичаФайлу(ПутьКФайлу);
-		
-		МодульМсп_Сервер = МодульМсп_Сервер();
-		Идентификатор = Запрос.Идентификатор;
-		Ванесса.УстановитьИдентификаторЗадачиMCP(Идентификатор);
-		МодульМсп_Сервер.УведомитьОПрогрессе(Идентификатор, 0, 3, Ванесса.Локализовать("Загрузка feature файла."));
-		
-		MCPOpenFeatureFileВРедакторе(ПутьКФайлу, ТипВызова, ДопПараметрыВызоваMCP);
+		MCPНачатьПроверкуПути(
+			ПутьКФайлу, Запрос, ТипВызова, ДопПараметрыВызоваMCP);
 	Иначе
 		ТекстОшибки = Ванесса.Локализовать("Не передан путь к файлу.");
 		МодульМсп_Сервер().ЗавершитьВызовИнструментаСОшибкой(Запрос.Идентификатор, "INVALID_PARAMS", ТекстОшибки);

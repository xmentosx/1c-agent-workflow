---
description: Show the ITL lifecycle panel for the current project context
agent: code
---

Run the helper lifecycle panel from the current project directory:

```powershell
powershell -ExecutionPolicy Bypass -File .\.agents\skills\1c-workflow\scripts\agent-1c.ps1 -Action help
```

On success, the entire final response must be exactly one fenced `text` code block with nothing outside it. Inside that block, return the source line and helper stdout as follows:

Required response format:

```text
Источник истины для текущей папки: helper lifecycle panel.

<paste the exact helper stdout here, unchanged>
```

Preserve every helper newline, blank line, and indentation inside the block. Do not summarize, translate, split into your own sections, reorder, omit, or merge helper sections. Preserve the helper's `Lifecycle:`, `ITL commands valid in this context:`, `Inherited by Kilo from primary checkout; invalid in this context:`, `OpenSpec:`, and `Additional helper actions:` sections exactly as printed. Do not add a separate "Lifecycle-действия не выполнялись" / "no lifecycle actions executed" note. If the helper fails, report the actual error instead of fabricating a panel. Do not execute a lifecycle action unless the developer clearly chooses one.

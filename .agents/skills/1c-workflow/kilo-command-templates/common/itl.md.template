---
description: Show the ITL lifecycle panel for the current project context
agent: code
---

Run the helper lifecycle panel from the current project directory:

```powershell
powershell -ExecutionPolicy Bypass -File .\.agents\skills\1c-workflow\scripts\agent-1c.ps1 -Action help
```

Return the helper stdout verbatim as the source of truth for the current folder.

Required response format:

```text
Источник истины для текущей папки: helper lifecycle panel.

<paste the exact helper stdout here, unchanged>
```

Do not summarize, translate, split into your own sections, reorder, omit, or merge helper sections. Preserve the helper's `Lifecycle:`, `ITL commands valid in this context:`, `Inherited by Kilo from primary checkout; invalid in this context:`, `OpenSpec:`, and `Additional helper actions:` sections exactly as printed. Do not add a separate "Lifecycle-действия не выполнялись" / "no lifecycle actions executed" note. Do not execute a lifecycle action unless the developer clearly chooses one.

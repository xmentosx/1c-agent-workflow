---
description: Показать жизненный цикл ITL для текущего контекста проекта
agent: code
---

Run the helper panel from the current project directory:

```powershell
powershell -ExecutionPolicy Bypass -File .\.agents\skills\1c-workflow\scripts\agent-1c.ps1 -Action help
```

On success, make the entire final response exactly one fenced `text` block with nothing outside it. Inside it, print the source line first, followed by the complete helper stdout:

Response format:

```text
Источник истины для текущей папки: helper lifecycle panel.

<вставьте сюда точный stdout helper без изменений>
```

Preserve every helper line break, blank line, and indentation. Do not paraphrase, translate, split it into your own sections, change the order, omit content, or merge sections. Preserve the sections `Жизненный цикл:`, `Команды ITL в этом контексте:`, `Унаследовано Kilo из основного checkout, но недоступно в этом контексте:`, `OpenSpec:`, and `Дополнительные действия:` exactly as printed. Do not add a separate message saying that lifecycle actions were not run. On failure, report the actual error instead of constructing the panel yourself. Do not run a lifecycle action until the developer explicitly selects it.

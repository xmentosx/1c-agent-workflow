---
description: Show available ITL 1C workflow actions
agent: code
---

Use the `1c-workflow` skill.

Show the 1C workflow menu from `.agents/skills/1c-workflow/references/workflow.md`, including new configuration branch, new extension branch, set extension name, dump extension files, update development branch base, refresh development branch, export development branch result, list development branches, switch master, and switch development branch actions.

Do not execute a lifecycle action unless the user clearly chooses one.
